# Uruhu Docker Development Environment
# Shared configuration for minimal, standard, and full Docker setups
# Compatible with WSL2 and Railway deployment

# =============================================================================
# CORE NETWORK CONFIGURATION
# =============================================================================
COMPOSE_PROJECT_NAME=uruhu
NETWORK_NAME=uruhu-network

# =============================================================================
# DATABASE CONFIGURATION - PostgreSQL
# =============================================================================
POSTGRES_DB=uruhu_dev
POSTGRES_USER=uruhu
POSTGRES_PASSWORD=dev_password
POSTGRES_INITDB_ARGS=--auth-host=scram-sha-256
POSTGRES_PORT=5432

# =============================================================================
# HEALTH CHECK CONFIGURATION
# =============================================================================

# PostgreSQL Health Check
POSTGRES_HEALTHCHECK_INTERVAL=20s
POSTGRES_HEALTHCHECK_TIMEOUT=10s
POSTGRES_HEALTHCHECK_RETRIES=5
POSTGRES_HEALTHCHECK_START_PERIOD=30s

# =============================================================================
# VOLUME CONFIGURATION
# =============================================================================
POSTGRES_VOLUME_NAME=uruhu-postgres-data

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Default Configuration Mode (minimal|standard|full)
DEFAULT_CONFIG_MODE=standard

# WSL2 Compatibility
WSL2_HOST=host.docker.internal